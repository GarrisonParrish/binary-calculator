"""Operations on binary numbers."""

def bin_addition():
    """Add two binary numbers (as lists) together and return the result as a list of integers."""
    # Which is faster, actually performing the calculation on the binary number or just adding the decimals and converting that result to binary?